services:
  # Host only the Frontend Dashboard as a static site.
  # The frontend will automatically run in local mode (connecting to localhost backend)
  # or transparently fall back to the fully-featured offline replay mode for presentations.
  - type: web
    name: voltgrid-dashboard
    env: static
    buildCommand: npm run build
    publishPath: dist
    root: dashboard
